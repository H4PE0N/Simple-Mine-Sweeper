
#ifndef MINE_SWEEPER_PROGRAM_H
#define MINE_SWEEPER_PROGRAM_H

bool mine_sweeper_game(bool*, Screen, const Bounds, Field, const Bounds);

bool game_still_running(bool*, Field, const Bounds);

#endif
